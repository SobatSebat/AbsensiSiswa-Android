<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.unpam.absensisiswa"
    android:versionCode="1"
    android:versionName="1.0.0">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher">

        <activity
            android:name="MainActivity"
            android:label="Absensi Siswa"
            android:noHistory="true"
            android:theme="@style/AppTheme.NoActionBar.SplashScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name="Login"
            android:label="Login Absensi"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name="MenuAbsensi"
            android:label="Menu Absensi"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name="AddGuruSiswa"
            android:label="Add Guru/Siswa"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name="AddKelas"
            android:label="Add Kelas"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name="AddMapel"
            android:label="Add Mata Pelajaran"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name="AddDaftar"
            android:label="Add Daftar Kelas dan Mapel"
            android:theme="@style/AppTheme">
        </activity>

    </application>

</manifest>
